# 힙
- 완전 이진트리에 있는 노드중에서 값이 가장 큰 노드(Max Heap)나 값이 가장 작은 노드(Min Heap)를 찾기 위해 만든 자료구조
- C# : priority queue(.NET6밖에 없어서 사용하려면 직접 구현해야함) 

- 힙의 불변성
    - 1. 최대/ 최소 원소에 즉각적으로 접근이 가능해야 한다.
    - 2. 부모 노드가 자식노드보다 항상 크거나(Max Heap) 작아야(Min Heap) 한다.

연산
    - 검색 및 읽기
        - 최대 / 최소 원소에 대해서만 가능하며 O(1)이다.
    - 삽입
        - 완전 이진 트리이기 때문에 O(logN)이다. 
    - 삭제
        - 최대 / 최소 원소에 대해서만 가능하며 O(logN)이다.